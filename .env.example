# ==============================================
# 班主任班级管理平台 - 环境变量配置
# ==============================================

# ==================== 应用基础配置 ====================
# 应用运行环境 (development, production, test)
NODE_ENV=development

# 应用访问地址 (用于认证回调和邮件链接)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 应用端口号 (可选，默认3000)
PORT=3000

# ==================== 数据库配置 ====================
# PostgreSQL 数据库连接字符串
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/class_tool_dev?schema=public"

# PostgreSQL 容器配置 (需与 docker-compose.yml 保持一致)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=class_tool_dev

# 数据库连接池配置 (可选)
# DATABASE_POOL_TIMEOUT=30000
# DATABASE_POOL_MAX=10

# ==================== Better Auth 认证配置 ====================
# 认证密钥 (生产环境必须修改为强密钥) openssl rand -hex 32
BETTER_AUTH_SECRET="your-secret-key-change-in-production-min-32-chars"

# 认证服务基础URL
BETTER_AUTH_URL="http://localhost:3000"

# JWT 令牌有效期 (秒，默认30天)
BETTER_AUTH_JWT_EXPIRES_IN=2592000

# 会话有效期 (秒，默认30天)
BETTER_AUTH_SESSION_EXPIRES_IN=2592000

# 是否禁用用户注册 (true: 禁用, false或留空: 启用)
NEXT_PUBLIC_DISABLE_SIGNUP=false

# 是否显示认证页面左侧装饰区域 (false: 隐藏, true或留空: 显示)
NEXT_PUBLIC_SHOW_AUTH_DECORATION=true

# ICP备案号 (留空则不显示)
NEXT_PUBLIC_ICP_NUMBER=

# 公安联网备案号 (留空则不显示)
NEXT_PUBLIC_POLICE_NUMBER=

# 公安联网备案查询链接 (可选)
NEXT_PUBLIC_POLICE_NUMBER_LINK=

# ==================== 邮件服务配置 (可选) ====================
# SMTP 邮件服务器配置 (用于邮箱验证和通知)
# EMAIL_FROM=noreply@yourschool.com
# EMAIL_SERVER_HOST=smtp.example.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@example.com
# EMAIL_SERVER_PASSWORD=your-email-password

# ==================== 第三方登录配置 (可选) ====================
# Google OAuth 配置
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth 配置 (开发测试用)
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# ==================== 文件存储配置 ====================
# 文件上传限制 (MB)
NEXT_PUBLIC_MAX_FILE_SIZE=10

# 支持的文件类型
NEXT_PUBLIC_ALLOWED_FILE_TYPES=.jpg,.jpeg,.png,.gif,.pdf,.xlsx,.xls

# 文件存储路径 (开发环境本地存储)
UPLOAD_DIR=./uploads

# ==================== 应用功能配置 ====================
# 班级最大学生数量
NEXT_PUBLIC_MAX_STUDENTS_PER_CLASS=100

# 积分系统配置
NEXT_PUBLIC_POINTS_MIN_VALUE=-999999
NEXT_PUBLIC_POINTS_MAX_VALUE=999999

# 随机点名避重时间 (小时)
NEXT_PUBLIC_CALL_AVOID_HOURS=24

# Excel 导入最大行数
NEXT_PUBLIC_EXCEL_MAX_ROWS=1000

# ==================== 日志配置 ====================
# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info

# 是否启用数据库查询日志
# DATABASE_LOGGING=true

# ==================== 监控配置 (生产环境) ====================
# 错误监控服务 (如 Sentry)
# SENTRY_DSN=your-sentry-dsn

# 性能监控
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# ==================== 缓存配置 (可选) ====================
# Redis 配置 (如需要缓存功能)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your-redis-password

# ==================== 备份配置 (生产环境) ====================
# 自动备份配置
# BACKUP_SCHEDULE="0 2 * * *"  # 每天凌晨2点备份
# BACKUP_RETENTION_DAYS=30
# BACKUP_STORAGE_PATH=/backups

# ==================== 开发调试配置 ====================
# 是否启用调试模式
# DEBUG=true

# 是否显示详细错误信息
# NEXT_PUBLIC_DEBUG_MODE=false

# 是否启用数据库种子数据
# SEED_DATABASE=true